<table>
  <thead>
    <tr>
      <th> Donors </th>
      <th> Value </th>
    </tr>
  </thead>
  <tbody>
    <% @project.donations.each do |pr| %>
    <tr>
      <td> <%= pr.user.try(:email) %> </td>
      <td> <%= pr.value %> </td>
    </tr>
  </tbody>
  <% end %>
</table>